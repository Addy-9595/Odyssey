# Expo/Metro resolves modules poorly through pnpm's isolated symlink tree, so
# the dashboard relies on a hoisted layout. This is the setup Expo documents for
# pnpm monorepos.
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
