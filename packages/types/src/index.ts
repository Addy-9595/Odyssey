// @odyssey/types — shared hand-authored TypeScript types that are NOT derived
// from the API contract (those come from @odyssey/api-client). Empty for the
// pipeline spike; populated as real shared types appear.
export {};
